<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            background-color: #000000;
            color: #ffffff;
        }
        .book-title-author {
            font-weight: bold;
            text-decoration: underline;
            margin-bottom: 5px;
        }
        .book-details {
            margin-left: 170px;
        }
    </style>
</head>
<body>
    <div>
        <h1>Dayo Lawal</h1>
        <header>Books I've READ</header>
    </div>
    <br>
</div>
<br>
<% books.forEach(book => { %>
    <div class="book-container" style="overflow: auto; margin-bottom: 20px;">
        <% if (book.cover_id) { %>
            <img src="<%= book.cover_id %>" alt="Book Cover" style="float: left; margin-right: 20px; width: 150px;" 
                 onerror="this.src='/default-cover.jpg'">
        <% } %>
        <div class="book-details">
            <p class="book-title-author"><%= book.title %> - <%= book.author %></p>
            <p style="font-size: 0.9em;">Published: <%= book.published_date %></p>
            <% if (book.notes) { %>
                <div class="notes">
                    <p style="font-style: italic;">Notes:</p>
                    <p><%= book.notes %></p>
                </div>
            <% } %>
        </div>
    </div>
<% }); %>
</body>
</html>